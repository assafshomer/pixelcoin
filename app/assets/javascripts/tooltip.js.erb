<% environment.context_class.instance_eval { include ViewsHelper } %>

$(function(){
	$('[title]').attr('rel','tooltip');
	$("[title][rel='tooltip']").attr('data-placement','top');
  $("[rel='tooltip']").tooltip();
});